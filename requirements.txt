# Dependencies for the benchmark model files
# (arima_model.py, sarima_model.py, prophet_model.py, lstm_model.py)
# Install with:  pip install -r requirements.txt
numpy>=1.26
pandas>=2.0
yfinance>=0.2.40
statsmodels>=0.14
arch>=6.3              # arima_model.py (GARCH volatility)
scikit-learn>=1.3
scipy>=1.10             # honest_eval (DM/PIT/KS/binomial stats)
matplotlib>=3.7
prophet>=1.1           # prophet_model.py
tensorflow>=2.12       # lstm_model.py

# Dependencies for calibration/regime (RegimeHMM engine)
hmmlearn>=0.3.0
pandas_ta>=0.3.14b

# Test tooling (models/ + calibration/regime test suites)
pytest>=7
pytest-timeout>=2

# model_artifacts/ (résidus ARIMA/SARIMA sérialisés en residuals.parquet)
pyarrow>=14
